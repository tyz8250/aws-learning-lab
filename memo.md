# AWS Learning Memo

## RDS

- RDSは、AWSが運用管理するマネージドデータベースサービス。
- Web EC2からは、RDSのエンドポイントとMySQLのポート（通常は`3306`）を指定して接続する。
- RDSのセキュリティグループでは、Web EC2のセキュリティグループからの接続だけを許可するのが基本。
- Multi-AZを有効にすると、別のAZにStandby DBが用意され、DBレイヤーの可用性が向上する。
- 障害時はStandbyへ自動フェイルオーバーする。接続先のエンドポイントは変わらないが、一時的な接続断は発生する。
- Multi-AZのStandbyは通常、読み取り処理には利用しない。読み取り性能を高める場合はRead Replicaを利用する。
